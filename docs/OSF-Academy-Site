================ Todo: ================

! PDP Future images not working

! PDP amounts JSON not working

! Replace overlay/cookies/signin
! Replace footer


Cart Page (interaction):
- Interactivity. Remove items (show as objects: Get info from JSON => make objects => show items) * Interaction with JSON needed: needed additional js file for each page + one general JSON file for buys

- Reorganize folders, images and paths
	Sass modules for repeated elements
	? Remove other colors from palette
	@mixin => to % @extends
	Repeated styles => to % @extends


Minor bugs:

! Fix subscribe form on release.html

! Firebase JSON contains empty items

! Crazy body/overlay

Bonuses:
Home/popular items:
- different types of items (w/w0 comments)
- collect all medias together if there's a need

================ Done: ================

Adaptive sections:
- Header
- Footer
- Benefits
- Featured products

Adaptive pages:
- 404
- Home
+ Release
+ CLP

JS
+ Home future dots slider
+ PDP small screen product images slider
+ JSON slider
+ Removed on styles of menu on closing header menu

----
+ Mobile: made icon boxes bigger
+ Provided link to Release page
+ Fixed Sign In
+ Fixed Release
+ Fixed no images
+ Fixed PDP Popular items
+ Fixed Breadcrumbs pages paths
---

